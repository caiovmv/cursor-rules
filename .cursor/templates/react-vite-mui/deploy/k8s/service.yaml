apiVersion: v1
kind: Service
metadata:
  name: mui-pro-app
  labels:
    app: mui-pro-app
spec:
  type: ClusterIP
  selector:
    app: mui-pro-app
  ports:
    - name: http
      port: 80
      targetPort: 4173
